// Remove the version number of the deliverables items in the table of contents.
(function() {
    /*
    const regex = /111\.1\.\d\./g;

    document
        .querySelectorAll('ol > li > ol > li > ol > li > a > strong')
        .forEach(item => item.innerText = item.innerText.replace(regex, ''));
    */
})();
